import day16 from './index';

const TEST_DATA = `
`;

describe('day16', () => {
  describe('first part', () => {
    it.skip('satisfies test data', () => {
      expect(day16('a', TEST_DATA)).toBe('2');
    });
  });
  describe('second part', () => {
    it.skip('satisfies test data', () => {
      expect(day16('b', TEST_DATA)).toBe('1');
    });
  });
});
